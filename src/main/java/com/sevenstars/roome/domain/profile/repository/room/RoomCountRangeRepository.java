package com.sevenstars.roome.domain.profile.repository.room;

import com.sevenstars.roome.domain.profile.entity.room.RoomCountRange;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoomCountRangeRepository extends JpaRepository<RoomCountRange, Long> {
}
